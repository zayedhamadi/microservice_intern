pipeline {
    agent any

    environment {
        IMAGE_NAME = "stage-gateway"
        IMAGE_TAG  = "${env.BUILD_NUMBER}"
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'dev',
                    credentialsId: 'github-account',
                    url: 'https://github.com/zayedhamadi/microservice_intern.git'
            }
        }
        stage('SonarQube Analysis') {
            steps {
                dir('gateway') {
                    script {
                        docker.image('maven:3.9-eclipse-temurin-21').inside("--network stage-network -v \$HOME/.m2:/root/.m2") {
                            withSonarQubeEnv('sonarqube') {
                                sh 'mvn clean verify sonar:sonar -Dsonar.projectKey=stage-gateway'
                            }
                        }
                    }
                }
            }
        }
        stage('Build Docker Image') {
            steps {
                dir('gateway') {
                    script {
                        docker.build("${IMAGE_NAME}:${IMAGE_TAG}")
                    }
                }
            }
        }
    }

    post {
        success { echo "Build réussi pour gateway #${env.BUILD_NUMBER}" }
        failure { echo "Échec du pipeline gateway #${env.BUILD_NUMBER}" }
    }
}
